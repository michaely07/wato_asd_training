control_node:
  ros__parameters:
    lookahead_distance: 1.0   # metres ahead on the path to target
    goal_tolerance: 0.1       # metres; stop when within this distance of final waypoint
    linear_speed: 0.5         # m/s constant forward speed
